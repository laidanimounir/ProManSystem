<UserControl x:Class="ProManSystem.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="Margin" Value="0,0,12,0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="15" ShadowDepth="3"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2D3748"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

       
        <Border Grid.Row="0" Background="White" CornerRadius="10" Padding="15">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Opacity="0.06" BlurRadius="10" ShadowDepth="2"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Width="50" Height="50" Background="#4299E1" CornerRadius="25" Margin="0,0,15,0">
                    <TextBlock Text="📊" FontSize="26" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel>
                    <TextBlock Text="Tableau de bord"
                               FontSize="22" FontWeight="Bold" Foreground="#1A202C"/>
                    <TextBlock Text="Vue globale des ventes, achats et stock"
                               FontSize="13" Foreground="#718096" Margin="0,2,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

       
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            
            <Border Grid.Column="0" Style="{StaticResource StatCardStyle}"
                    ToolTip="Total des ventes TTC du mois en cours">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#4299E1" Offset="0"/>
                        <GradientStop Color="#3182CE" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Width="48" Height="48" Background="#FFFFFF30" CornerRadius="24" 
                            HorizontalAlignment="Left" Margin="0,0,0,12">
                        <TextBlock Text="💰" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <StackPanel Grid.Row="1">
                        <TextBlock Text="Ventes du mois" Foreground="#E6F2FF" FontSize="13" FontWeight="Medium"/>
                        <TextBlock x:Name="SalesMonthTtcText"
                                   FontSize="28" FontWeight="Bold"
                                   Foreground="White"
                                   Margin="0,4,0,0"/>
                    </StackPanel>

                    <TextBlock Grid.Row="2" Text="Total factures TTC"
                               FontSize="11" Foreground="#B3D9FF" Margin="0,8,0,0"/>
                </Grid>
            </Border>

          
            <Border Grid.Column="1" Style="{StaticResource StatCardStyle}"
                    ToolTip="Total des achats TTC du mois en cours">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#48BB78" Offset="0"/>
                        <GradientStop Color="#38A169" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Width="48" Height="48" Background="#FFFFFF30" CornerRadius="24" 
                            HorizontalAlignment="Left" Margin="0,0,0,12">
                        <TextBlock Text="🛒" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <StackPanel Grid.Row="1">
                        <TextBlock Text="Achats du mois" Foreground="#E6F9F0" FontSize="13" FontWeight="Medium"/>
                        <TextBlock x:Name="PurchasesMonthTtcText"
                                   FontSize="28" FontWeight="Bold"
                                   Foreground="White"
                                   Margin="0,4,0,0"/>
                    </StackPanel>

                    <TextBlock Grid.Row="2" Text="Total factures achat"
                               FontSize="11" Foreground="#B3F5D1" Margin="0,8,0,0"/>
                </Grid>
            </Border>

           
            <Border Grid.Column="2" Style="{StaticResource StatCardStyle}"
                    ToolTip="Nombre de factures de vente ce mois">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#ED8936" Offset="0"/>
                        <GradientStop Color="#DD6B20" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Width="48" Height="48" Background="#FFFFFF30" CornerRadius="24" 
                            HorizontalAlignment="Left" Margin="0,0,0,12">
                        <TextBlock Text="📄" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <StackPanel Grid.Row="1">
                        <TextBlock Text="Factures vente" Foreground="#FFF5E6" FontSize="13" FontWeight="Medium"/>
                        <TextBlock x:Name="SalesInvoicesCountText"
                                   FontSize="28" FontWeight="Bold"
                                   Foreground="White"
                                   Margin="0,4,0,0"/>
                    </StackPanel>

                    <TextBlock Grid.Row="2" Text="Ce mois"
                               FontSize="11" Foreground="#FFE0B3" Margin="0,8,0,0"/>
                </Grid>
            </Border>

           
            <Border Grid.Column="3" Style="{StaticResource StatCardStyle}" Margin="0"
                    ToolTip="Produits dont le stock actuel est inférieur au stock minimum">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#F56565" Offset="0"/>
                        <GradientStop Color="#E53E3E" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Width="48" Height="48" Background="#FFFFFF30" CornerRadius="24" 
                            HorizontalAlignment="Left" Margin="0,0,0,12">
                        <TextBlock Text="⚠️" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <StackPanel Grid.Row="1">
                        <TextBlock Text="Stock bas" Foreground="#FFF5F5" FontSize="13" FontWeight="Medium"/>
                        <TextBlock x:Name="LowStockProductsCountText"
                                   FontSize="28" FontWeight="Bold"
                                   Foreground="White"
                                   Margin="0,4,0,0"/>
                    </StackPanel>

                    <TextBlock Grid.Row="2" Text="Produits en alerte"
                               FontSize="11" Foreground="#FFC7C7" Margin="0,8,0,0"/>
                </Grid>
            </Border>
        </Grid>

       
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

          
            <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="20">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" Opacity="0.08" BlurRadius="12" ShadowDepth="2"/>
                </Border.Effect>
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="📋" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="Dernières factures de vente"
                                   Style="{StaticResource SectionTitle}" Margin="0"/>
                    </StackPanel>

                    <DataGrid x:Name="LastSalesGrid"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              CanUserAddRows="False"
                              SelectionMode="Single"
                              BorderThickness="0"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#E2E8F0"
                              AlternatingRowBackground="#F7FAFC"
                              RowHeight="36">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#2D3748"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="FontSize" Value="13"/>
                                <Setter Property="Padding" Value="12,8"/>
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="N°" Binding="{Binding NumeroFacture}" Width="90"/>
                            <DataGridTextColumn Header="Client" Binding="{Binding CustomerName}" Width="*"/>
                            <DataGridTextColumn Header="Date"
                                                Binding="{Binding DateFacture, StringFormat=dd/MM/yyyy}"
                                                Width="110"/>
                            <DataGridTextColumn Header="Montant TTC"
                                                Binding="{Binding MontantTTC, StringFormat=N2}"
                                                Width="120"/>
                            <DataGridTemplateColumn Header="Statut" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="4" Padding="8,4" HorizontalAlignment="Center">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#FED7D7"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding EstPayee}" Value="True">
                                                            <Setter Property="Background" Value="#C6F6D5"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock FontWeight="SemiBold" FontSize="11">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Text" Value="Non payée"/>
                                                        <Setter Property="Foreground" Value="#C53030"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding EstPayee}" Value="True">
                                                                <Setter Property="Text" Value="Payée"/>
                                                                <Setter Property="Foreground" Value="#2F855A"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

           
            <Border Grid.Column="2" Background="White" CornerRadius="12" Padding="20">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" Opacity="0.08" BlurRadius="12" ShadowDepth="2"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="⚠️" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="Stock faible"
                                   Style="{StaticResource SectionTitle}" Margin="0"/>
                    </StackPanel>

                    
                    <DataGrid Grid.Row="1" x:Name="LowStockGrid"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              CanUserAddRows="False"
                              SelectionMode="Single"
                              BorderThickness="0"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#FED7D7"
                              AlternatingRowBackground="#FFF5F5"
                              RowHeight="34">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#C53030"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="FontSize" Value="12"/>
                                <Setter Property="Padding" Value="10,6"/>
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Code" Binding="{Binding CodeProduit}" Width="75"/>
                            <DataGridTextColumn Header="Produit" Binding="{Binding Nom}" Width="*"/>
                            <DataGridTextColumn Header="Stock"
                                                Binding="{Binding StockActuel, StringFormat=N1}"
                                                Width="60">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#E53E3E"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Min"
                                                Binding="{Binding StockMin, StringFormat=N1}"
                                                Width="60"/>
                        </DataGrid.Columns>
                    </DataGrid>

                   
                    <Border Grid.Row="1" x:Name="NoLowStockMessage"
                            Visibility="Collapsed"
                            Background="#E6FFFA"
                            BorderBrush="#38B2AC"
                            BorderThickness="2"
                            CornerRadius="8"
                            Padding="20">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="🎉" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Text="Aucun produit en alerte"
                                       FontSize="14" FontWeight="Bold"
                                       Foreground="#234E52"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="Tous les stocks sont au niveau optimal"
                                       FontSize="11" Foreground="#4A5568"
                                       HorizontalAlignment="Center"
                                       Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
